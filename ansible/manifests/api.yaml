---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stratologia-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stratologia-api
  template:
    metadata:
      labels:
        app: stratologia-api
    spec:
      containers:
        - name: api
          image: giannoskomninos/stratologia-api:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: "stratologia-db"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "stratologia_db"
            - name: DB_USER
              value: "stratologia_user"
            - name: DB_PASS
              value: "stratologia_pass"
            - name: SMTP_HOST
              value: "stratologia-mailhog"
            - name: SMTP_PORT
              value: "1025"
            - name: APP_BASE_URL
              value: "https://it22050.duckdns.org"
            - name: KEYCLOAK_AUTH_URL
              value: "https://it22050-auth.duckdns.org"
            - name: KEYCLOAK_BACKEND_URL
              value: "http://stratologia-keycloak:8080"
            - name: KEYCLOAK_CLIENT_SECRET
              value: "UtTbGbbh0RpxlxBvbMXojemYK1YCYWgY"

---
apiVersion: v1
kind: Service
metadata:
  name: stratologia-api
spec:
  selector:
    app: stratologia-api
  ports:
    - port: 5000
      targetPort: 5000
